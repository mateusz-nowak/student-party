@if(Request.IsAuthenticated) {
    <text>Witaj <strong>@User.Identity.Name</strong>!
    [ @Html.ActionLink("Wyloguj", "LogOff", "Account") ]</text>
}
else {
    @:[ @Html.ActionLink("Zaloguj", "LogOn", "Account") ]
    @:[ @Html.ActionLink("Zarejestruj", "Register", "Account") ]
}
